version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.7.0

    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

      # run tests with coverage!
      - run: yarn coverage

      #- store_artifacts:
      #    path: coverage

  deploy:
    machine:
      enabled: true

    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: Deploy Master to Heroku
          command: |
            if [ "$CIRCLE_BRANCH" == master ]; then
              echo "REACT_APP_SERVER_URL"=$(echo $PROD_REACT_APP_SERVER_URL) >> .env
              scp .env $DEPLOY_USER@$STAG_IP:/home/$DEPLOY_USER/$STAGING_DEPLOY_FOLDER/.env
              ssh $DEPLOY_USER@$STAG_IP "./deploy.sh"
            else
             echo "REACT_APP_SERVER_URL"=$(echo $STAG_REACT_APP_SERVER_URL) >> .env
            fi

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only:
                - develop
                - master
